#include<stdio.h>
int main(){

    // ||(OR) Logical Operator:- Checks if atleast one conditon is true

    float temp =-1000;

    if(temp <=0 || temp >=30){
        printf("the whether is bad");
    } else{
        printf("The whether is good");
    }
}